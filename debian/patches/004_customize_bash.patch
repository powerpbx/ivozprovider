Index: ivozprovider/profiles/proxy/root/.bashrc.d/550-proxy-system-custom
===================================================================
--- /dev/null
+++ ivozprovider/profiles/proxy/root/.bashrc.d/550-proxy-system-custom
@@ -0,0 +1,20 @@
+# Custom aliases
+
+ # override previous alias set in common package
+unalias sc
+
+ ## EXAMPLES
+# "sc status ku" is alias for "systemctl --no-pager status kamailio@users.service -l"
+# "sc status kt" is alias for "systemctl --no-pager status kamailio@trunks.service -l"
+# "sc status firewalld" is alias for "systemctl status firewalld"
+
+ sc ()
+{
+	if [[ $2 == 'ku' ]]; then
+		systemctl --no-pager $1 kamailio@users.service -l
+	elif [[ $2 == 'kt' ]]; then
+		systemctl --no-pager $1 kamailio@trunks.service -l
+	else
+		systemctl --no-pager $1 $2 -l
+	fi
+}
Index: ivozprovider/profiles/common/root/.bashrc.d/150-common-custom
===================================================================
--- /dev/null
+++ ivozprovider/profiles/common/root/.bashrc.d/150-common-custom
@@ -0,0 +1,2 @@
+# Add custom aliases for frequently used commands
+alias sc='systemctl'
